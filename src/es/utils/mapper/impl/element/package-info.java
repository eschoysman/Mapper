/**
 * Provides a class handling the mapping of a single element of a class.<br>
 * The package contains the following class:
 * <ul>
 * <li>{@code ElementMapper} : contains the informations and the logic needed the execute the mapping of an single element</li>
 * </ul>
 * @author eschoysman
 * @see es.utils.mapper.impl.element.ElementMapper
 */
package es.utils.mapper.impl.element;